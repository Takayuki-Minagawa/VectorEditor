# VectorEditor 追加機能の提案と作業計画

作成日: 2026-09-06

調査対象: ローカルリポジトリ `42ba4a1`（package.json: v1.1.0、Unreleased の実装を含む）

状態: 段階0〜1（N01〜N04）をv1.2.0として実装。N05〜N10は次段階の候補として継続管理。

## 実施記録（2026-09-06）

| 項目 | 状態 | 実装・検証内容 |
|---|---|---|
| N01 出力プリセット | 実装済み | 名前付き保存・呼出し・更新・削除、モード別管理、入力検証、選択なしの出力抑止。再読込後のPNG実寸とCAD PDF用紙寸法をE2Eで確認 |
| N02 一括バックアップ | 実装済み | 対象プロジェクト・保存世代・素材をJSON出力し、全件検証・件数表示後に別コピー復元。DB間の途中失敗・容量不足時の整理と次回起動時の復旧を検証 |
| N03 実寸合わせ | 実装済み | 基準2点・既知長さ・mm/cm/m、基準点固定の等方変形、複数選択、連動寸法再計算、Undo・JSON保存復元。ズーム・パン後の操作もE2Eで確認 |
| N04 幾何計測 | 実装済み | 2点距離、3点角度、閉図形の面積・外周・穴周長、単位切替とコピー。回転・不等倍率・穴付き図形・退化入力を検証 |
| N05〜N10 | 未着手 | 当初計画どおり段階1の利用結果を踏まえて優先度を見直す |

追加コードは既存のFabricオブジェクトとschema v2を利用しており、今回の文書schema変更はない。出力プリセットとバックアップのコンテナ形式は独立したversion 1を持つ。

実装上の範囲:

- バックアップは保存済みデータが対象。編集中の変更はプロジェクトへ保存してから出力する。初版は非圧縮JSON、最大100 MiB、プロジェクト・素材各1,000件、各プロジェクトの保存世代は最大20件。
- 一括復元は新IDを持つ別コピーとして追加する。図面内部のオブジェクトIDは図面単位で保持し、寸法・コネクタの参照関係を保つ。Web Locks対応ブラウザでは複数タブのバックアップ処理を直列化する。
- 計測の曲線は既存の幾何変換公差で近似する。重なり合う領域は先に合体し、開いた線・自己交差は面積計測の対象外とする。
- 実寸合わせは遠近補正を行わない。ロック済みの一般図形は変更せず、異常な倍率や図面サイズ超過を拒否する。

ローカル検証:

- `npm run check`: ESLint、型検査、58ファイル・450件の単体/コンポーネントテスト、本番ビルドが成功。
- `npm run test:e2e -- --workers=2`: 既存12件と追加6件の計18シナリオ。
- `npm run audit:prod`: high/criticalは0件。既存の間接依存`dompurify`と`fflate`にmoderateが各1件あり、今回の依存バージョン変更には含めない。
- 日本語の出力ダイアログと幅768pxのバックアップダイアログをスクリーンショットで確認。

GitHub Actionsの利用を抑える公開手順:

1. 実装・検証はローカルで行い、途中pushをしない。
2. 既存Pagesワークフローに`workflow_dispatch`を追加する。既存の品質ゲートと通常のpushトリガーは維持する。
3. 公開対象コミットに`[skip ci]`を付けてpushし、自動CIと自動Pagesデプロイの重複実行を避ける。
4. 最後にPagesワークフローだけを手動で1回起動し、完了と公開ページを確認する。

このスキップ指定がpush/pull_requestイベントに適用されることは[GitHub公式資料](https://docs.github.com/en/actions/how-tos/manage-workflow-runs/skip-workflow-runs)で確認した。

## 1. 推奨方針

**最初に「出力プリセット」「一括バックアップ」「画像・トレース図形の実寸合わせ」「角度・面積計測」を追加する。** 既存の描画・保存・出力処理を活用でき、挿絵作成とCAD利用の双方で作業時間を減らせるため、初回リリースの候補とする。

次に、CAD画層とDXF読み込みを整備して外部図面を扱えるようにする。ベジェペン、オフセット等の編集機能、複数ページ化は用途に応じて段階的に追加する。

優先順位はコードと既存の用途から判断した提案であり、利用頻度の実測やユーザーヒアリングによる確定順位ではない。既存の [追加機能.md](./追加機能.md) は実装状況・バックログとして残し、本書で着手範囲、依存関係、完了条件を具体化する。

## 2. 現状と追加する価値

| 領域 | 確認できた実装 | 今回追加する価値 |
|---|---|---|
| 描画 | 図形、ノードとベジェハンドルの編集、ブール演算、数値移動・複写 | ベジェペンによる新規曲線、CADの部分編集 |
| CAD | mm基準座標、端点・中点・中心・交点スナップ、連動寸法線 | 実寸校正、角度・面積計測、属性付き画層 |
| 画像 | ブラウザ内ベクター化、中心線抽出、清書、プレビュー | 読み込んだ図の縮尺合わせ、直接貼り付け |
| 断面 | 断面生成、穴・切欠き、凸角フィレット、面積・重心・断面諸量 | 既存の幾何処理を一般図形の計測にも活用 |
| 保存 | JSON、IndexedDB自動保存、名前付きプロジェクト、最大20世代、素材保存 | 複数プロジェクト・世代・素材をまとめて別環境へ移せるバックアップ |
| 出力 | SVG/PNG/PDF、CADのR12 ASCII DXF出力 | 設定の再利用、DXF読み込み、複数ページPDF |
| 文書 | 単一キャンバス、schema v2、文書全体のUndo/Redo | 複数ページの管理と互換性のある保存形式 |

画像ベクター化、ノード編集、ブール演算、断面計算、プロジェクト世代保存は既に実装されているため、新規機能として重複提案しない。現在のレイヤーパネルはオブジェクトの階層・表示・ロック管理であり、共通属性を持つCAD画層とは区別する。

## 3. 追加機能一覧と優先順位

P1: 初回候補、P2: 次段階、P3: 大きな文書設計変更を伴う後続候補。工数は担当者1名が既存コードを理解して作業する場合の概算人日で、設計・実装・関連テスト・説明更新を含む。外部ライブラリの適合性や実図面の複雑さにより再見積もりする。

| ID | 追加機能 | 優先度 | 主な効果 | 概算人日 | 依存関係 |
|---|---|---|---|---:|---|
| N01 | エクスポート設定のプリセット保存 | P1 | 同じ提出形式・用紙・縮尺をすぐ再利用 | 2–3 | 既存ExportService |
| N02 | プロジェクト・世代・素材の一括バックアップ／復元 | P1 | ブラウザ外への保管と環境移行 | 5–8 | 既存Repository、文書検証 |
| N03 | 画像・トレース図形の実寸合わせ | P1 | 紙図面やスケッチをCAD寸法へ合わせる | 4–6 | 座標変換、既存トレース・履歴 |
| N04 | 長さ・角度・周長・面積の計測 | P1 | 寸法確認・数量把握を画面内で完結 | 4–6 | 既存スナップ、閉図形の幾何処理 |
| N05 | 外部画像・SVGの直接貼り付け | P2 | 資料作成時のファイル保存・読込操作を削減 | 3–5 | 既存importと内部コピーの整理 |
| N06 | 色・線種・印刷可否を持つCAD画層 | P2 | 建築図面の用途別管理と出力制御 | 7–10 | 文書schema拡張 |
| N07 | DXF読み込み（対応要素を限定） | P2 | 既存CAD図面を編集・再利用 | 8–12 | N06。幾何変換の調査は先行可能 |
| N08 | オフセット・トリム・延長 | P2 | 壁芯・補助線・図面修正の反復操作を短縮 | 10–16 | 幾何処理、連動参照の更新方針 |
| N09 | ベジェペンとノード種別切替 | P2 | 挿絵やトレース輪郭を滑らかな曲線で作成 | 6–10 | 既存ノード編集・DrawingSession |
| N10 | 複数ページと複数ページPDF | P3 | 図面一式・複数枚の教材を1文書で管理 | 12–20 | 文書・履歴・保存・出力の設計変更 |

## 4. 機能別の作業内容と完了条件

### N01 エクスポート設定のプリセット保存

- 作業: 形式、出力範囲、余白、背景・透過、PNG倍率、CAD用紙・向き・縮尺を名前付きで保存し、選択・更新・削除できるようにする。
- 保存: 図面本体ではなくブラウザ内の利用者設定とする。プリセット形式にバージョンを持たせ、読込時に数値と列挙値を検証する。
- UI: イラスト/CADで適用できない項目を区別し、選択対象がない場合は「選択範囲」の出力を抑止する。ファイル名は毎回変更できる。
- 主な変更先: `src/components/ExportDialog.tsx`、設定保存用ユーティリティを新設。
- 完了条件: 再起動後に設定を呼び出せる。未保存時の出力操作を維持し、プリセット適用後もプレビュー・実出力のサイズと背景が一致する。
- 検証: 設定の保存・破損時復帰の単体テスト、保存→再読込→PDF/PNG出力のE2E。

### N02 一括バックアップ／復元

- 作業: 対象プロジェクトの最新文書、保存済み世代、素材を、バージョン付きの単一JSONバックアップへ出力する。初版では圧縮やクラウド同期を含めない。
- 復元: 書込前に全件を検証し、件数・サイズ・復元対象を表示する。初版は既存データを上書きせず別コピーとして取り込む。元のdocumentIdと世代の紐付けを新IDへ変換する。
- 注意点: プロジェクトと素材は別のIndexedDBにあるため、単一トランザクションで一括確定できない。復元ジョブの記録と新規作成IDの追跡により、途中失敗・再起動時の清掃または再開を設計する。
- 主な変更先: `src/services/projectRepository.ts`、`src/services/symbolRepository.ts`、`src/components/ProjectManager.tsx`、バックアップ用サービスを新設。
- 完了条件: 新しいブラウザ環境で文書・世代・素材を復元できる。内部の寸法・コネクタ参照が維持される。容量不足や不正ファイルで既存データを壊さず、再試行で重複が増えない。
- 検証: 複数プロジェクトの往復、ID衝突、古い文書の移行、DB間の途中失敗、保存容量不足、再起動後の復旧。

### N03 画像・トレース図形の実寸合わせ

- 作業: CADモードで画像または選択図形群上の2点を指定し、既知の長さをmm/cm/mで入力する。2点間距離から等方倍率を求め、第1点を固定して選択全体を拡大縮小する。
- 範囲: 配置済み画像とトレース結果の両方を対象とする。初版は遠近補正や縦横別倍率を扱わない。
- 整合性: mmの文書座標で計算し、画面ズームやPDF印刷縮尺とは別に扱う。寸法・コネクタは幾何変更後に既存の更新処理を通す。
- 主な変更先: `src/components/Canvas.tsx`、`src/components/PropertyPanel.tsx`、`src/utils/canvasCommands.ts`、校正ダイアログ・計算関数を新設。
- 完了条件: 2点間200の図形に既知長さ1,000mmを設定すると5倍になり、第1点の位置は動かない。Undo 1回で戻り、保存・復元後も寸法が一致する。
- 検証: mm/cm/mの同値入力、回転・グループ・ズーム・パン、ゼロ距離、不正値、処理中キャンセル、連動寸法の更新。

### N04 長さ・角度・周長・面積の計測

- 作業: 2点間距離、3点の角度、選択した閉図形の周長・面積を表示する。穴がある図形の面積は穴を控除し、周長は外周と穴周長を区別して表示する。
- 既存との差: `MeasurePopup`の座標・幅・高さ表示や、断面に変換した後の面積表示に加え、作図中の一般的な計測操作を提供する。
- 範囲: 初版の面積計測は既存の幾何変換が受け付ける閉図形に限定する。自己交差・開図形・未対応図形には結果を出さず理由を示す。曲線近似には既存公差を使用する。
- 主な変更先: `src/components/MeasurePopup.tsx`、`src/domain/tools.ts`、`src/utils/drawingGeometry.ts`、`src/utils/sectionGeometry.ts`。
- 完了条件: 100×200mmの矩形で面積20,000mm²、周長600mmを表示する。cm/mへの切替は面積の二乗換算も正しい。測定のオーバーレイが保存・出力に混入しない。
- 検証: 既知の矩形・三角形・穴付き図形、回転、不等倍率、角度の退化ケース。コピーした数値にも単位を付ける。

### N05 外部画像・SVGの直接貼り付け

- 作業: 編集画面のpasteイベントで画像・SVGを判定し、既存import処理を共通サービスへ抽出して配置する。
- 操作設計: アプリ内コピーとOSクリップボードの優先順位を明示する。文字編集中・入力欄・ダイアログでは通常の貼り付けを維持する。
- 範囲: 取得できるMIMEデータに対応し、未対応ブラウザ向けには既存のファイル読込を案内する。SVGの外部参照・複雑さの検査を既存読込にも共通適用する。
- 主な変更先: `src/hooks/useKeyboardShortcuts.ts`、`src/components/Toolbar.tsx`、importサービスを新設。
- 完了条件: 対応する外部PNG/SVGを1操作で配置でき、IDを再採番し、Undo 1回で取り消せる。内部コピーの寸法参照や素材の情報を失わない。
- 検証: 内部/外部貼り付けの競合、テキスト編集中、空データ、巨大画像、不正SVG。OS由来データは実ブラウザでも確認する。

### N06 CAD画層

- 作業: `layerId`と文書内画層定義を追加する。名前、色、線種、線幅、表示、ロック、印刷可否を管理し、オブジェクト属性は「画層に従う／個別指定」を選べるようにする。
- 保存: schema移行を先に実装し、既存文書は既定画層へ割り当てる。オブジェクトのZ順と画層所属は別に保持する。
- 整合性: グループ内の所属・継承ルール、非表示と印刷不可の違いを定義する。履歴、JSON、素材挿入、各形式の出力に同じ解決規則を適用する。
- 主な変更先: `src/types.ts`、`src/utils/fabricObjectMetadata.ts`、`src/utils/documentSerializer.ts`、`src/components/LayerPanel.tsx`、`src/services/exportService.ts`、`src/services/dxfExporter.ts`。
- 完了条件: 画層の色変更が所属図形に反映され、個別指定は維持される。印刷不可の補助線はPDF/PNG/SVGに出ない。DXFへの印刷可否の扱いは対応範囲を明示する。
- 検証: v1/v2文書の移行、Undo/Redo、グループ、削除時の所属移動、素材挿入時の画層名衝突、表示と出力の組合せ。

### N07 DXF読み込み

- 作業: パーサー候補のライセンス・ブラウザ動作・入力制限を調査した上で、初版はR12 ASCIIの2D `LINE`、直線辺の`POLYLINE`、`CIRCLE`、`TEXT`、基本画層情報に限定する。
- 読込フロー: 対応/未対応要素数、外接範囲、入力単位をプレビューする。単位が不明なら利用者の指定を必須とし、内部mmへ変換する。
- 範囲: DWG、3D、BLOCK/INSERT、曲線bulge等は初版で変換せず、件数と理由を報告する。日本語TEXTの文字コードは検証用図面で調べ、対応範囲を決める。
- 主な変更先: `src/services/dxfImporter.ts`を新設、`src/components/Toolbar.tsx`、`src/utils/objectIds.ts`、N06の画層モデル。
- 完了条件: 対応図面を編集可能な図形として読み込み、単位・向き・位置関係を保持する。既存DXF出力との往復で対応要素の座標・寸法が許容誤差内に収まる。途中失敗はキャンバスへ反映しない。
- 検証: 小規模基準図面、日本語、負座標、大座標、壊れた入力、未対応要素の混在。大規模入力は上限・キャンセル・Worker化の要否を測定する。

### N08 オフセット・トリム・延長

- 作業順: 直線の平行オフセット → 直線同士のトリム/延長 → 開閉ポリラインのオフセットの順に実装する。
- 範囲: 初版は直線と直線辺のポリライン。曲線、壁の接合修復、断面専用オブジェクトは後続とする。対象外の選択には明示的に案内する。
- 整合性: 交点候補のプレビュー、平行線・微小辺の処理、自己交差の拒否を実装する。頂点番号が変わる操作では寸法・コネクタの参照を更新し、対応不能なら参照解除と通知を行う。
- 主な変更先: 幾何演算用ユーティリティを新設、`src/utils/canvasCommands.ts`、`src/utils/semanticObjects.ts`、`src/domain/tools.ts`。
- 完了条件: 指定距離の平行線生成と境界線までの切断・延長ができる。連続操作中もプレビューを取り消せ、確定操作ごとにUndoできる。
- 検証: 平行・交差・端点接触、鋭角、反転、重複点、非対応オブジェクト、参照先の削除・分割。

### N09 ベジェペンとノード種別切替

- 作業: クリックで直線アンカー、ドラッグで曲線ハンドルを作成するペンツールを追加する。開いたパスの確定・閉鎖・取消と、Cusp/Smooth/Symmetricの切替を追加する。
- 既存との差: 既存パスのアンカー・ハンドル編集とセグメント分割は再利用する。新規パスの作成とハンドル間の制約を補う。
- 主な変更先: `src/domain/tools.ts`、`src/hooks/useDrawingSession.ts`、`src/utils/nodeEditing.ts`、`src/utils/pathCommands.ts`、`src/components/PropertyPanel.tsx`。
- 完了条件: 作成した曲線をノード編集で再調整できる。Smoothは接線方向、Symmetricは方向とハンドル長を保つ。1パスの作成をUndo 1回で戻せる。
- 検証: 閉鎖、取消、短いパス、種別切替、変形済みパス、保存・復元、SVG出力の曲線維持。

### N10 複数ページと複数ページPDF

- 作業: 初版は独立キャンバスをページ切替する方式とし、ページの追加・複製・並替え・削除、ページ名、寸法設定を追加する。並列アートボード表示は後続とする。
- 設計: `pages[]`とアクティブページIDを導入し、旧文書は1ページへ移行する。文書全体のUndoにページ構成を含め、各ページの表示状態をどう保持するか定義する。
- 出力: オフスクリーン出力をページごとに呼び出し、全ページ/指定ページのPDFを生成する。SVG/PNGは初版では現在ページのみとする。
- 主な変更先: `src/types.ts`、`src/store/useEditorStore.ts`、`src/utils/documentSerializer.ts`、`src/utils/historyService.ts`、保存系サービス、`src/services/exportService.ts`、ページ一覧UIを新設。
- 完了条件: 異なるサイズの3ページを保存・復元・並替えでき、PDFの枚数・順序・実寸が一致する。最終1ページは削除不可とし、ページ複製ではオブジェクトIDとページ内参照を再対応付けする。
- 検証: 旧schema、ページ切替中の自動保存、削除後のUndo、ページ複製、バックアップ復元、画像の多い文書のメモリ消費。初版ではページをまたぐ寸法・コネクタ参照を作らない。

## 5. 実施順序と成果物

| 段階 | 内容 | 概算人日 | 成果物・進行条件 |
|---|---|---:|---|
| 0 | 現行テストの基準確認、代表図面・画像・旧文書の検証データ準備、画面と保存仕様の確定 | 2–3 | 検証データ、UI案、schema変更一覧、工数の再見積もり |
| 1 | N01 → N02 → N03 → N04 | 15–23 | 最小リリース候補。各機能の完了条件と保存・出力の回帰確認を満たす |
| 2 | N05 → N06 → N07 | 18–27 | 外部データの取込みと画層管理。DXFの対応要素を明記した説明書 |
| 3 | N08 → N09 | 16–26 | CAD編集と曲線作成。各操作の取消・参照更新が検証済み |
| 4 | N10 | 12–20 | 複数ページ文書、移行処理、複数ページPDF |

段階0〜1は合計17〜26人日で、1人専任ならおおむね4〜6週間を目安とする。全候補は合計63〜99人日で、兼務・レビュー待ち・利用者評価期間は別途必要。すべてを一括実装する前提にはせず、段階1の利用結果を見て以後の優先度を見直す。挿絵用途が中心ならN09を段階2へ繰り上げ、CAD用途が中心ならN06〜N08を優先する。

各機能は以下の単位で進める。

1. 対象操作・対象外・保存項目・完了条件を確定する。
2. 純粋な計算/変換処理と入力検証を実装する。
3. UI、文書履歴、保存、既存の参照更新処理へ組み込む。
4. 対象機能の単体/E2Eと関連する保存・出力の回帰確認を行う。
5. 日本語/英語UI、ヘルプ、README、既存バックログの状態を更新する。

## 6. 共通設計と品質確認

- **互換性:** 現在の文書schemaはv2。N06とN10は導入順に新しいバージョンと移行処理を定義する。途中バージョンだけでなくv1/v2からも検証し、未対応の新形式を黙って読み捨てない。
- **履歴:** 図面を変更する操作は確定1回につき1トランザクション。プレビューは永続データに含めず、取消・失敗時は元の状態を保つ。利用者設定やバックアップ操作は図面のUndoと分離する。
- **IDと参照:** 複写・読込時のID再採番と寸法/コネクタ参照の対応付けを同時に扱う。単なる図形変換だけで完了としない。
- **保存先:** JSON、自動保存、プロジェクト世代、素材、バックアップ間で、新しい文書属性が失われないことを確認する。
- **性能:** 現行の入力上限、履歴50件/約32MiBの制約と合わせて測定する。大規模図面向けの性能目標は段階0で代表データと測定環境を決めてから設定する。
- **UI:** 既存Dialog・コマンドパレット・ツール登録を活用し、日本語/英語、キーボード、入力中のショートカット抑止に対応する。
- **出力:** ズーム・パンによらず結果が一致し、CADの実寸が保持される。新しい属性を扱えない形式には制限を表示する。

実装時の確認コマンド:

```bash
npm ci
npm run check
npm run test:e2e
npm run audit:prod
```

E2Eの初回環境準備には `npx playwright install chromium` を使用する。当初の計画作成時は文書・ソースコード・既存CI構成を調査し、今回の段階0〜1の実装後に上記コマンドをローカル実行した。結果は冒頭の実施記録に記載する。

## 7. 今回の推奨範囲から外す候補

- **クラウド共同編集:** 認証、競合解決、アクセス権、サーバー運用が必要。まずN02でデータの持出し・移行を整備する。
- **DWG対応:** 形式変換とライセンスの検討が大きいため、限定したDXF読み込みで需要と互換性を確認する。
- **OCR・多色ベクター化:** 既存トレースとは別の精度評価が必要。対象画像、日本語精度、モデルの配布サイズを先に検証する調査候補とする。
- **材料強度・部材耐力等の構造設計計算:** 現行の幾何断面計算から用途が広がるため、本計画には含めない。

## 8. 調査根拠

外部サービスやライブラリの最新仕様を前提にした選定は行わず、以下のローカル資料・コードを根拠に作成した。新しい依存ライブラリの選定は該当機能の着手時に公式資料で確認する。

| 確認事項 | 主な参照先 |
|---|---|
| 実装済み機能・バックログ | [README.md](./README.md)、[追加機能.md](./追加機能.md) |
| 単一文書モデル・schema v2・読込検証 | [types.ts](./src/types.ts)、[documentSerializer.ts](./src/utils/documentSerializer.ts) |
| 出力ダイアログの設定 | [ExportDialog.tsx](./src/components/ExportDialog.tsx) |
| import対応形式 | [Toolbar.tsx](./src/components/Toolbar.tsx) |
| プロジェクト・世代・素材の保存構造 | [projectRepository.ts](./src/services/projectRepository.ts)、[symbolRepository.ts](./src/services/symbolRepository.ts)、[ProjectManager.tsx](./src/components/ProjectManager.tsx) |
| 計測UI・スナップ・連動参照 | [MeasurePopup.tsx](./src/components/MeasurePopup.tsx)、[cadSnapping.ts](./src/utils/cadSnapping.ts)、[semanticObjects.ts](./src/utils/semanticObjects.ts) |
| ツール・ノード編集・貼付操作 | [tools.ts](./src/domain/tools.ts)、[nodeEditing.ts](./src/utils/nodeEditing.ts)、[useKeyboardShortcuts.ts](./src/hooks/useKeyboardShortcuts.ts) |
| DXF出力・確認コマンド | [dxfExporter.ts](./src/services/dxfExporter.ts)、[package.json](./package.json)、[ci.yml](./.github/workflows/ci.yml) |
