# 作業計画: ノード編集(パス編集)とブーリアン演算

作成日: 2026-07-28
対象: ベジェ曲線・ラインの中間ノード追加(ポリライン化/セグメント分割)、ノードのマウス移動、図形同士のブーリアン演算(合体・差分・交差・除外)

## 実施状況(2026-07-28 実装完了)

- **Phase 1(ノード表示・ドラッグ移動): 完了** — `nodeEdit` ツールを追加。Line / Polyline / Polygon / Path に fabric v7 の `controlsUtils`(`createPolyControls` / `createPathControls`)+ 自作 Line コントロールで頂点ハンドルを装着。グリッド吸着対応、1ジェスチャ = 1履歴。
- **Phase 2(ノード追加・削除): 完了** — セグメントのダブルクリックで挿入(Line→Polyline 変換、ベジェは de Casteljau 分割で形状保存)、ノードのダブルクリックで削除。`vertexIndex` 参照のシフト補正・Line アンカーの再バインド実装済み。
- **Phase 3(ブーリアン4演算): 完了** — `convertObject` に閉パス対応(evenodd を polygon-clipping XOR で解決)を追加し、`intersect` / `xor` をカーネルへ追加。`shapeBoolean.ts` の4演算コマンドをコンテキストメニュー / コマンドパレットへ配線。キーボードショートカットは既存割当との衝突回避のため見送り(パレットから実行可能)。
- **Phase 4(ノードタイプ・セグメント変換): 一部実装** — ベジェ制御ハンドルのドラッグ編集は `createPathControls` により実装済み。Cusp/Smooth/Symmetric 切替、直線⇔曲線変換、パス切断/結合は未着手(計画どおり別イテレーション)。
- **Phase 5(仕上げ): 完了** — 単体テスト(pathCommands / nodeEditing / shapeBoolean)、E2E(node-editing.spec.ts)、i18n 日英、README・追加機能.md・ヘルプ更新。
- **実装中の追加知見**: fabric v7 はオブジェクトの `originX/Y` デフォルトが `center`。また `lockMovementX/Y` は履歴シリアライズ時に `locked` メタデータへ橋渡しされるため、ノード編集中の移動抑止はロックフラグではなく `object:moving` ガードで実装した。

---

## 1. 背景と目的

`追加機能.md` のバックログにある以下の項目を、一般的なベクターエディタ(Inkscape / Illustrator / Figma / Affinity Designer)の標準挙動に合わせて実装する。

- Polygon / Polyline のノード編集
- ベジェペンとアンカー/ハンドル編集(のうち、既存図形の編集部分)
- 図形のブール演算(合体、差分、交差)

## 2. ウェブ調査まとめ: 一般的なエディタの標準機能

### 2.1 ノード編集(Inkscape ノードツール / Illustrator アンカーポイントツール相当)

| 機能 | 標準的な操作方法 | 備考 |
|---|---|---|
| ノード(頂点)の表示 | ノード編集ツールでオブジェクトをクリック | 四角=コーナー、菱形/円=スムーズ等の形状で種別を表示 |
| ノードのドラッグ移動 | ノードをマウスでドラッグ | Ctrl で水平/垂直拘束、グリッド/OSNAPスナップ併用 |
| **ノード追加(セグメント分割)** | **セグメント上をダブルクリック**(Inkscape)、またはツールバーの「+」ボタン | 挿入時は**形状を変えない**のが標準。直線は単純分割、ベジェは de Casteljau 分割で隣接ハンドルを調整 |
| ノード削除 | ノード選択 + Delete | 削除後も形状をなるべく保つ(Inkscape)/単純に頂点を除去(Ctrl+Del) |
| 複数ノード選択 | クリック、Shift+クリック、ラバーバンド | 選択ノードの一括移動 |
| パス切断(break) | 選択ノードで切断 | 1ノードを2つの非接続端点に分割 |
| ノード結合(join) | 2端点を1ノードにマージ / セグメントで接続 | 開パス同士の連結 |
| セグメントの直線⇔曲線変換 | ツールバーボタン | Line セグメント ⇔ Cubic Bezier セグメント |
| ノードタイプ変更 | Cusp(角) / Smooth(滑らか) / Symmetric(対称) / Auto | ベジェハンドル間の拘束の違い |
| ハンドル編集 | ハンドル端点をドラッグ | ベジェ制御点の操作 |

### 2.2 ブーリアン演算(Figma / Illustrator Pathfinder / Inkscape パスメニュー)

| 演算 | Figma | Illustrator | 挙動 |
|---|---|---|---|
| 合体 | Union | Unite | 全図形の外形を統合 |
| 差分 | Subtract | Minus Front | 最背面(または対象)から前面図形を引く |
| 交差 | Intersect | Intersect | 重なり部分のみ残す |
| 除外 | Exclude | Exclude | 重なり部分を除いた部分を残す(XOR) |

共通仕様:
- 2つ以上の閉じた図形が対象。開パス(線)は対象外またはエラー
- 結果は穴(hole)を含み得る → evenodd / リング構造で表現
- スタイルは通常、最背面(Subtract)または最前面(その他)の図形から継承
- Figma は非破壊(グループ保持)、Illustrator/Inkscape は破壊的が基本。**本プロジェクトは破壊的(結果パスに置換)を採用**(既存の断面ブーリアンと同じ)

### 2.3 参考ソース

- [Inkscape Beginners' Guide — Node Tool Options](https://inkscape-manuals.readthedocs.io/en/latest/node-operations.html)
- [Inkscape Beginners' Guide — Editing Paths with the Node Tool](https://inkscape-manuals.readthedocs.io/en/latest/editing-paths.html)
- [Tavmjong Bah — Inkscape Manual: Editing Paths](http://tavmjong.free.fr/INKSCAPE/MANUAL/html/Paths-Editing.html)
- [Figma Learn — Boolean operations](https://help.figma.com/hc/en-us/articles/360039957534-Boolean-operations)
- [Figma Plugin API — BooleanOperationNode](https://www.figma.com/plugin-docs/api/BooleanOperationNode/)
- [Paper.js — PathItem(unite / intersect / subtract / exclude)](https://paperjs.org/reference/pathitem/)
- [Path Bool ライブラリ README(曲線を保持するブーリアンの例)](https://huggingface.co/spaces/openfree/graphite2/blob/main/libraries/path-bool/README.md)

## 3. 現状のコードベースとの接点(前提)

- ドキュメントモデルは Fabric.js オブジェクトツリーのみ。ライン=`fabric.Line`、ポリライン=`fabric.Polyline`/`fabric.Polygon`、ベジェは `fabric.Path`(現状ペンシル描画・断面・トレース出力のみで、編集機能なし)
- ブーリアンは断面プロファイル限定で実装済み: `src/utils/sectionBoolean.ts`(polygon-clipping ラップ、union / difference のみ。intersection / xor は未実装だがライブラリ自体は対応)
- 曲線→折れ線化(適応分割)は `src/utils/sectionGeometry.ts` の `appendAdaptiveArc` に前例あり。ただし `fabric.Path` / `Polyline` / `Line` は未対応
- ツール追加規約: `src/types.ts` の `ToolType` → `src/domain/tools.ts` の `TOOL_DEFINITIONS` → i18n キー(型で網羅チェック)
- 一時状態は `src/hooks/useDrawingSession.ts` の判別 union、オーバーレイ描画は `after:render`(`src/components/Canvas.tsx`)
- 履歴はフルスナップショット方式。コミットは `executeCanvasTransaction`(`src/utils/canvasCommands.ts`)経由で「1ジェスチャ = 1 push」
- 新規永続プロパティは `FABRIC_CUSTOM_PROPERTIES`(`src/utils/fabricObjectMetadata.ts`)+ `documentSerializer.ts` のバージョン/マイグレーションをセットで更新
- 頂点参照の基盤(`SemanticAnchor.vertexIndex`、OSNAP の頂点候補生成)は既存。**ノード編集で頂点数が変わると寸法線/コネクタの `vertexIndex` がずれる**点に注意

## 4. 技術選定

### 4.1 ブーリアン演算カーネル

**方針: 既存の polygon-clipping を継続採用し、曲線は適応分割で折れ線化してから演算する(MVP)。**

| 選択肢 | 長所 | 短所 |
|---|---|---|
| **polygon-clipping(既存)+ 事前折れ線化** | 依存追加なし。断面ブーリアンの検証済みラッパー(`sectionBoolean.ts`)・トポロジ検証・テスト資産を再利用できる | 曲線が折れ線化される(結果はポリゴン)。分割密度と頂点数のトレードオフ |
| paper.js の PathItem ブーリアン | 曲線(ベジェ)を保持したまま unite/subtract/intersect/exclude が可能 | 依存が大きい(レンダラ込み)。Fabric との座標/パス変換層が必要。エッジケースのバグ報告あり |
| Path Bool 系(曲線対応の専用ライブラリ) | 曲線保持・軽量 | 実績・保守状況の見極めが必要 |

CAD 用途(断面・寸法)では折れ線化は既に許容済み(`approximate` フラグの前例あり)。イラスト用途で「曲線のまま」の結果が必要になった時点で paper.js 等への差し替えを検討する(カーネルを `sectionBoolean.ts` 同様に薄いラッパーで隔離しておく)。

### 4.2 ノードのドラッグ移動

**方針: Fabric.js のカスタムコントロール API(v6+ の `controlsUtils.createPolyControls` 相当)を第一候補とし、`Line` / `Path` は同 API で自作コントロールを定義する。**

- Fabric 公式のポリゴン頂点編集手法(各頂点に control を割り当て、`pathOffset` を補正しながら `points` を更新)が Polyline/Polygon にそのまま使える
- 採用できない/挙動が合わない場合のフォールバック: `DrawingSession` に `nodeEditing` 状態を追加し、OSNAP マーカーと同じ `after:render` オーバーレイ + 独自ヒットテストで実装(コード量は増えるが完全に制御できる)
- どちらの場合も、ドラッグ確定(`mouse:up`)で `executeCanvasTransaction` により1回だけ履歴 push する

## 5. 作業フェーズ

### Phase 1: ノード編集ツールの基盤(ノード表示 + ドラッグ移動)

**対象図形: `Polyline` / `Polygon` / `Line`**

1. `ToolType` に `nodeEdit` を追加し、`TOOL_DEFINITIONS`(category: `basic`, gesture: `select`)、i18n、ツールパネルアイコンを整備
2. `src/utils/nodeEditing.ts`(新規): 図形種別ごとの「頂点列の読み書き」抽象を定義
   - `getEditableNodes(obj): { point: Point; index: NodeRef }[]`(ドキュメント座標系。flipX/flipY は `readSectionProfileRingsInDocumentCoordinates` の作法を踏襲)
   - `moveNode(obj, ref, newPoint)` / `commitNodes(obj)`(Polyline の `pathOffset` / バウンディング再計算を内包)
3. ノード編集モード時に選択オブジェクトへ頂点コントロールを装着(`toolActivation.ts` の `configureCanvasForTool` を拡張)
4. ドラッグ中の挙動: グリッドスナップ(`snapVal`)・OSNAP(`cadSnapping.ts`)・Ctrl での直交拘束(`applyOrtho`)を既存ユーティリティで適用
5. 確定時: `executeCanvasTransaction` で履歴 push、`updateLinkedSemanticObjects` で寸法線/コネクタを追従更新
6. 頂点参照の整合: `SemanticAnchor.vertexIndex` を持つ寸法線/コネクタがある図形で頂点を移動した場合の追従を確認

**受け入れ基準**: ポリライン/ポリゴン/ラインの頂点をドラッグで移動でき、Undo/Redo が1操作単位で効き、保存/復元後も形状が正しい。

### Phase 2: ノード追加・削除

**対象: Phase 1 の図形 + `fabric.Path`(ベジェ)**

1. **ライン上のノード追加 → ポリライン化**
   - ノード編集モードで `fabric.Line` のセグメントを**ダブルクリック**すると、その点に頂点を挿入し、オブジェクトを `fabric.Polyline`(2→3点)へ**種別変換**する
   - 変換時に `id` / メタデータ(`fabricObjectMetadata`)・スタイルを引き継ぎ、`selectedObjectIds` と OSNAP・寸法参照の `objectId` を維持
2. **ポリライン/ポリゴンのノード追加**
   - セグメントのダブルクリックで、クリック位置(セグメント上への射影点)に頂点を挿入
   - 挿入位置より後ろの頂点を参照する `SemanticAnchor.vertexIndex` をシフト補正するユーティリティを `semanticObjects.ts` 側に追加
3. **ベジェ(`fabric.Path`)のノード追加**
   - パスコマンド配列(`M/L/C/Q/Z`)を解析する `src/utils/pathCommands.ts`(新規)を実装
   - セグメントのダブルクリック位置に対し、直線セグメントは線形分割、`C`/`Q` は **de Casteljau 分割**(パラメータ t で2つの曲線に分割し、形状を変えずにアンカーを挿入 — Inkscape と同じ挙動)
   - クリック点→曲線上の最近傍 t の算出は、既存 `appendAdaptiveArc` と同様のサンプリング + 局所ニュートン法程度で十分
4. **ノード削除**
   - 選択ノード + Delete で頂点除去(ポリラインは単純除去、ベジェは隣接セグメントを1本の曲線に接続)。最小頂点数(線=2、ポリゴン=3)を下回る操作は拒否しトースト通知
5. ダブルクリックのルーティング: `Canvas.tsx` の `handleDblClick` にノード編集モード分岐を追加

**受け入れ基準**: ライン/ポリライン/ベジェの中間に、形状を変えずにノードを挿入・削除でき、挿入直後にそのノードをドラッグできる。

### Phase 3: ブーリアン演算の汎用図形対応

1. **図形→ポリゴンリング変換の拡充**(`src/utils/sectionGeometry.ts`)
   - `convertObject` に `fabric.Path`(`C`/`Q`/`A` は `appendAdaptiveArc` と同じ適応分割 + `MAX_CURVE_VERTICES` ガード)と `Polygon` 以外の閉図形の対応を追加
   - 開パス(`Line` / 開いた `Polyline` / 開いた `Path`)は対象外としてエラーコード `open-path` を返す(一般的エディタと同じ制約)
2. **演算の追加**(`src/utils/sectionBoolean.ts` またはこれを汎用化した `src/utils/shapeBoolean.ts`)
   - 既存: union / difference。追加: `intersectionProfiles`(polygon-clipping の `intersection`)、`xorProfiles`(`xor`)
   - 既存のエラーコード体系(`no-intersection` / `empty-result` 等)・面積検算・トポロジ検証(`sectionTopology.ts`)を流用
3. **コマンド層**(`src/utils/canvasCommands.ts` または `sectionCommands.ts` の隣に `shapeBooleanCommands.ts`)
   - `uniteSelection` / `subtractSelection`(最背面を subject、他を cutter — Illustrator の Minus Front 相当)/ `intersectSelection` / `excludeSelection`
   - 結果は `createSectionPath` と同型の `fabric.Path`(`fillRule: 'evenodd'`、リングは `M/L/Z`)として生成し、元図形を削除(破壊的)。スタイルは subject(差分)/最前面(その他)から継承
   - `keepSources` オプション(元図形を残す)を既存の `SectionOperationOptions` に合わせて用意
4. **UI 導線**: `ContextMenu.tsx` / `CommandPalette.tsx` / ツールバーに4演算を追加(2つ以上の閉図形選択時のみ有効化)。ショートカットは `useKeyboardShortcuts.ts`(例: Ctrl+Alt+U / - / I / X は既存割当と衝突確認の上で決定)
5. 断面プロファイル同士の既存演算(`sectionCommands.ts`)との関係を整理: 選択に sectionProfile が含まれる場合は既存の断面演算を優先するか、統一メニューに寄せるかを実装時に判断(推奨: 内部カーネルを共通化し、メニューは1系統に統一)

**受け入れ基準**: 矩形・円・多角形・ベジェパスの任意の組合せで4演算が実行でき、穴を含む結果が正しく描画・保存・SVG/PDF/DXF 出力される。

### Phase 4: ノードタイプとセグメント変換(発展・任意)

一般的エディタとの同等性を高める追加機能。Phase 1–3 の完了後に着手判断。

1. ベジェハンドルの表示とドラッグ編集(`fabric.Path` のアンカーに対する制御点コントロール)
2. ノードタイプ: Cusp / Smooth / Symmetric の切替とハンドル拘束
3. セグメントの直線⇔曲線変換
4. パス切断(1ノード→2端点)と端点結合(join)
5. ノードのラバーバンド複数選択と一括移動

### Phase 5: 仕上げ(横断作業)

1. **シリアライズ**: 新規メタデータが必要になった場合のみ `FABRIC_CUSTOM_PROPERTIES` + `DOCUMENT_VERSION` マイグレーションを追加(基本方針: Fabric ネイティブの `points` / `path` に載せて追加プロパティを避ける)
2. **テスト**
   - 単体(Vitest): `pathCommands`(de Casteljau 分割の形状保存性 — 分割前後の曲線サンプリング点の一致)、`nodeEditing`(pathOffset/flip 下の座標往復)、ブーリアン4演算(穴・自己接触・非交差・面積検算。既存 `sectionBoolean.test.ts` のパターンを流用)
   - E2E(Playwright): ノード編集ツールでの「ダブルクリック挿入 → ドラッグ → Undo → Redo」、ブーリアン実行 → 保存/再読込
3. **i18n**: 日英のラベル・トースト文言
4. **ドキュメント**: `README.md` の機能一覧と `追加機能.md` のバックログ消し込み

## 6. リスクと対策

| リスク | 対策 |
|---|---|
| Polyline の `pathOffset` / 変換行列(scale/flip/angle)下での頂点座標の往復誤差 | `readSectionProfileRingsInDocumentCoordinates` の既存実装を流用し、往復プロパティテストを先に書く |
| ノード挿入/削除による `vertexIndex` 参照(寸法線・コネクタ)のずれ | index シフト補正ユーティリティを Phase 2 の必須要件に含める。補正不能な参照は最近傍頂点へ再バインド |
| Line → Polyline の種別変換で `objectId` 依存箇所が壊れる | id・メタデータ引き継ぎを `shapeFactory` 側の共通関数にし、変換を1トランザクションで実施 |
| ブーリアンの数値不安定(既知: polygon-clipping の桁落ち) | 既存の「bbox 中心平行移動 + 面積検算 + `numerical-instability` エラー」の作法をそのまま流用 |
| 曲線折れ線化による品質低下(イラスト用途) | MVP は折れ線で出し、カーネルをラッパーで隔離。将来 paper.js / Path Bool へ差し替え可能な構造にする |
| `Canvas.tsx`(1226行)の肥大化 | ノード編集ロジックは新規フック `useNodeEditing.ts` + `src/utils/nodeEditing.ts` に隔離し、`Canvas.tsx` へはイベント配線のみ追加 |

## 7. 実施順序と目安

1. Phase 1(ノード表示・移動): 基盤 + 座標往復テスト — **最優先**
2. Phase 2(ノード追加・削除): ライン→ポリライン化、de Casteljau 分割
3. Phase 3(ブーリアン4演算): カーネル拡張 → コマンド → UI
4. Phase 5(仕上げ)は各 Phase 完了ごとに随時実施
5. Phase 4(ハンドル編集・ノードタイプ)は別イテレーションで判断

各 Phase は独立してマージ可能な単位とし、1 Phase = 1 PR を基本とする。
