# 追加機能提案

VectorEditor（v1.0.3）に追加価値の高い機能を、4つのサブエージェントで観点を分けて調査し、メインエージェントで統合した結果です。

- **技術スタック**: React 19 / TypeScript / Fabric.js 7 / Zustand / jsPDF / svg2pdf.js / KaTeX
- **調査観点**: ①描画・編集 ②CAD・建築/製図 ③ファイル入出力・相互運用 ④UI/UX・協調作業
- **調査日**: 2026-06-21

---

## エグゼクティブサマリ（最優先推奨）

低コストかつ即効性が高く、既存基盤を活かせる順に並べた「まず着手すべき」候補です。

| 優先 | 機能 | 観点 | 難易度 | 既存基盤 |
|------|------|------|--------|----------|
| ★1 | システムクリップボード連携（コピペ） | 入出力/編集 | 低 | `clipboard` state 実装済み |
| ★2 | フリーハンド描画（鉛筆/ブラシ） | 描画 | 低 | Fabric `PencilBrush` |
| ★3 | 直交モード/角度スナップ（Ortho） | CAD | 低 | 回転15度拘束ロジック流用 |
| ★4 | オブジェクトスナップ（端点/中点/交点 OSNAP） | CAD | 中 | `getScenePoint` |
| ★5 | ダークモード/テーマ切替 | UI/UX | 低 | 要CSS変数化 |
| ★6 | トースト通知（alert置換） | UI/UX | 低 | — |
| ★7 | 自動保存の復元確認・スナップショット | 入出力 | 低〜中 | `AutoSaveData.savedAt` |

「製図ツール」としての実用精度を出すには ★3・★4 の角度/オブジェクトスナップが事実上の必須機能です。挿絵エディタの核としては下表「ベジェ/ペンツール」「コネクタ線」がインパクト大です。

---

## 1. 描画ツール・オブジェクト編集

| # | 機能 | 概要 | 価値 | 難易度 |
|---|------|------|------|--------|
| 1 | **ベジェ/ペンツール** | アンカー点＋ハンドルで曲線を描画、既存パスのノード編集 | 任意曲線・有機的形状が描けない最大の欠落。ロゴ/カスタム矢印に必須 | 高 |
| 2 | **フリーハンド描画（鉛筆/ブラシ）** | ドラッグで自由曲線。Fabric `PencilBrush`で容易 | 手書き注釈・スケッチ。低コスト高効果 | 低 |
| 3 | **スポイト（スタイルコピー）** | 既存オブジェクトの色/線/スタイルを抽出して適用 | 配色統一・スタイル再利用の高速化 | 中 |
| 4 | **グラデーション塗り** | 線形/放射グラデーション。Fabric `Gradient`標準対応 | 立体感・質感表現。背景/ボタンの表現力向上 | 中 |
| 5 | **コピー/ペースト（Ctrl+C/V配線）** | `clipboard` stateは定義済みだがC/Vハンドラ未配線 | 標準操作の欠落。土台が整っており低コスト | 低 |
| 6 | **反転（左右/上下フリップ）** | `flipX`/`flipY`で即実装 | 矢印/対称図形作成で頻用 | 低 |
| 7 | **ブール演算（合体/型抜き/交差）** | 2図形の和差積で新形状生成 | アイコン/ロゴ制作。要パス演算ライブラリ | 高 |
| 8 | **ドロップシャドウ/ぼかし** | `fabric.Shadow`標準対応 | 図解・UIモックの見栄え向上 | 低〜中 |
| 9 | **コネクタ線（図形追従の結線）** | 2図形を結ぶ線が移動に追従 | フロー図/組織図/構成図で必須級。差別化要素 | 高 |
| 10 | **リッチテキスト/パス追従テキスト** | 図形内折り返し、パス沿いテキスト、文字単位の色 | 吹き出し/ラベル/装飾見出しの表現力向上 | 中 |

**関連ファイル**: `Canvas.tsx`, `ToolPanel.tsx`, `PropertyPanel.tsx`, `types.ts`, `store/useEditorStore.ts`, `hooks/useKeyboardShortcuts.ts`

---

## 2. CADモード・建築/製図

| # | 機能 | 概要 | 価値 | 難易度 |
|---|------|------|------|--------|
| 1 | **直交モード/角度スナップ（Ortho & Polar）** | 線/壁/寸法線を水平垂直/15度刻みに拘束 | 製図の最頻出操作。現状は斜めになりやすく実用性を損なう | 低 |
| 2 | **オブジェクトスナップ（端点/中点/交点 OSNAP）** | 描画中に既存頂点へ吸着＋マーカー表示 | AutoCAD/Jw_cadの中核。壁/寸法線の連結精度が出ない致命的欠落 | 中 |
| 3 | **連続線/連続壁（一筆書き入力）** | 壁/線をクリックで繋いで連続入力 | `wall`が1ドラッグ1矩形で間取り作成が手間。polygon累積ロジック流用可 | 中 |
| 4 | **寸法線の引出線・自動追従値** | 補助線（Extension Line）付与、長さ変更で値自動更新 | 現状は2点直線＋固定ラベルでJIS/ISO製図に不十分 | 中 |
| 5 | **角度・面積の計測ツール** | 2線分の角度、部屋面積（m²/坪）を計測 | `measure`は矩形LaTeX座標専用。建築で需要大 | 低〜中 |
| 6 | **CAD的レイヤー（画層：色/線種/非印刷）** | 通り芯/壁/寸法を画層分け、画層プロパティ制御 | 既存レイヤーパネルに画層属性が無い。図面整理に有効 | 中〜高 |
| 7 | **ブロック/シンボルライブラリ** | ドア/窓/通り芯/家具の再利用シンボル配置 | 建築図面作成を大幅高速化。`reassignObjectIds`クローン基盤あり | 中 |
| 8 | **カーソル座標・長さ/角度のリアルタイム表示** | ステータスバーに実寸座標、描画中に長さ/角度ツールチップ | CADの基本UX。現状StatusBarに座標表示なし | 低 |
| 9 | **ガイド線の数値指定/個別削除・原点設定** | mm指定追加、個別削除（`removeGuide`未配線）、原点移動 | 相対寸法の基準として有用 | 低 |
| 10 | **DXF/DWGインポート・エクスポート** | 業界標準DXFで外部CADと相互運用 | 実務価値最大級だがパーサ実装コスト高 | 高 |

**関連ファイル**: `Canvas.tsx`, `StatusBar.tsx`, `Rulers.tsx`, `CadExportDialog.tsx`, `NumericMoveDialog.tsx`, `store/useEditorStore.ts`, `types.ts`

---

## 3. ファイル入出力・データ相互運用

| # | 機能 | 概要 | 価値 | 難易度 |
|---|------|------|------|--------|
| 1 | **システムクリップボード連携** | OSクリップボード経由で画像/SVGを外部と相互コピペ | Slack/ドキュメントへ即貼付、外部図の直接取込。頻度が極めて高い | 低〜中 |
| 2 | **自動保存の復元確認・複数スナップショット** | 無確認上書きを改善、名前付き数世代のlocalStorage保持 | 白紙上書き事故の防止、簡易巻き戻し（現状historyはリロードで消失） | 低〜中 |
| 3 | **テンプレート/複数ドキュメント管理** | localStorageに複数の名前付きドキュメントを保存・一覧 | 単一スロット制約の解消、定型挿絵/CAD枠の再利用 | 中 |
| 4 | **DXFエクスポート** | CADの線/矩形/円/寸法をDXF（R12 ASCII）出力 | AutoCAD/JW_CADへの受け渡し。CADメタデータが既に揃い着手容易 | 中〜高 |
| 5 | **DXF/DWGインポート** | 外部CADデータをfabricオブジェクトへ取込 | 既存CAD図面ベースの作成。エクスポートと対で相互運用完結 | 高 |
| 6 | **クラウド連携（Google Drive保存/読込）** | JSONプロジェクトをクラウド保存・共有URL発行 | 端末間同期・共有。要OAuth/バックエンド | 高 |
| 7 | **選択範囲のみ/透過/解像度指定エクスポート** | 選択のみ・余白トリミング・倍率/透過を選択可能に | アイコン切り出し、Web用透過素材出力 | 低〜中 |
| 8 | **複数ページPDF/一括バッチ書き出し** | 複数アートボード/選択を連番・複数ページPDFへ | 連作挿絵・図面セットの効率出力 | 中 |

**関連ファイル**: `Toolbar.tsx`, `hooks/useAutoSave.ts`, `store/useEditorStore.ts`, `CadExportDialog.tsx`, `types.ts`, `App.tsx`, `hooks/useKeyboardShortcuts.ts`

---

## 4. UI/UX・アクセシビリティ・協調作業

| # | 機能 | 概要 | 価値 | 難易度 |
|---|------|------|------|--------|
| 1 | **ダークモード/テーマ切替** | ライト/ダークをトグル、CSS変数で配色一元管理 | 眼精疲労軽減、現代エディタの標準。要CSS変数化（現状色ハードコード） | 低 |
| 2 | **レイヤーパネル強化（検索/D&D並替/リネーム）** | 検索ボックス、ドラッグでZ順変更、ダブルクリックでリネーム | オブジェクト多数時の識別性。`object.name`保存対応済みで素地あり | 中 |
| 3 | **アクセシビリティ対応** | aria-label、フォーカストラップ、Escクローズ、コントラスト確保 | スクリーンリーダー/キーボード操作。教材の公的納品要件に対応 | 中 |
| 4 | **テンプレート/素材ライブラリ** | フローチャート/間取り部品/建具シンボル等のギャラリー | ゼロから描く負担削減。SVGインポート基盤を再利用可能 | 中 |
| 5 | **オブジェクト検索/コマンドパレット** | Ctrl+Fでテキスト/レイヤー検索、Ctrl+Kで操作検索実行 | 多数オブジェクトで目的物に素早く到達。学習コスト低減 | 低〜中 |
| 6 | **ミニマップ/ナビゲーター** | 全体縮小ビュー＋ビューポート枠表示・クリック移動 | CADモードのパン/ズームと好相性。大図面での迷子防止 | 中 |
| 7 | **トースト通知** | 保存完了/コピー/エラーを非モーダルなトーストで通知 | 現状の`alert()`ブロッキングを改善、自動保存の無音状態を解消 | 低 |
| 8 | **協調機能（共有リンク/注釈コメント）** | 段階導入：共有URL生成→コメントピン→（将来）CRDT共同編集 | 提案書/教材のレビュー協業。完全同時編集は要WebSocket/Yjs | 高 |

**番外（小規模・効果的）**: モバイル/タッチ対応（中）、ショートカットのカスタマイズ（中）、ヘルプの検索（低）

**関連ファイル**: `App.css`, `LayerPanel.tsx`, `StatusBar.tsx`, `Toolbar.tsx`, `ContextMenu.tsx`, `HelpManual.tsx`, `hooks/useKeyboardShortcuts.ts`, `store/useEditorStore.ts`

---

## 推奨ロードマップ（フェーズ別）

**フェーズ1（クイックウィン・低コスト高効果）**
- システムクリップボード連携、コピペ配線、反転、フリーハンド描画
- 直交/角度スナップ、カーソル座標表示
- ダークモード、トースト通知、自動保存の復元確認

**フェーズ2（実用精度・差別化）**
- オブジェクトスナップ（OSNAP）、連続壁、寸法線引出線、面積計測
- スポイト、グラデーション、ドロップシャドウ
- レイヤーパネル強化、テンプレート/素材ライブラリ

**フェーズ3（高インパクト・高コスト）**
- ベジェ/ペンツール、コネクタ線、ブール演算
- DXFインポート/エクスポート、クラウド連携、協調編集
