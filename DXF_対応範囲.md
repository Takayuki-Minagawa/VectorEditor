# 外部データ・CAD画層・DXF対応範囲（v1.3.0）

実装日: 2026-09-06。作業ブランチ: `codex/interoperability-cad-layers`。

## 外部画像・SVGの貼り付け

選択ツールでCtrl/⌘+Vを押すと、OSクリップボードから取得できるPNG/JPEG/GIF/WebP、SVGファイルまたはSVG文字列を追加する。SVGを優先し、外部データを新たにコピーした場合に古い内部コピーを貼り付けない。内部コピーの寸法参照・ID・画層定義は保持し、貼り付け先ではIDを再採番する。コンテキストメニューのコピーもOS側に内部コピーの識別情報を置く。

入力欄・Fabric文字編集中・モーダルダイアログでは通常の貼り付けを維持する。ブラウザが画像データを渡さない場合は「インポート」からファイルを指定する。同じ検証・配置処理をファイル読込にも適用した。

- ファイルは最大20 MiB、ラスター画像は40,000,000ピクセル・一辺32,767px以下。
- SVGは最大10,000要素、100,000パス命令。参照展開20,000要素・深さ50を上限とし、循環参照を拒否する。
- スクリプト・イベント属性・foreignObject・外部画像/スタイル参照・DTD等を読み込み前に拒否する。ローカルのグラデーション・参照には対応する。
- 外部画像/SVGは見えている領域の中央へ、必要な場合のみ縮小して配置する。画層0の個別指定で元の見た目を保持する。
- 1回のUndoで追加を取り消す。読み込み中にUndo・文書切替が発生した場合は古い読み込み結果を破棄する。

## CAD画層

既存のオブジェクト一覧・Z順とは別に、文書内で最大200画層を管理する。画層0は削除できない。名前、色、線種（実線/破線/点線）、線幅、表示、ロック、印刷・出力可否を保存する。

- 「画層に従う」は線色・線幅・線種を画層から解決し、文字・矢印先端の色も合わせる。図形の塗り色は保持する。
- 「個別指定」は継承前の属性へ戻す。一般プロパティのスタイル編集は、その時点の属性を個別指定に切り替える。グラデーションも保存・復元する。画像パターンの線色は個別指定で扱う。
- 名前付きのアクティブ画層では、新規作図を画層に従わせる。画層0では作図ツールの個別スタイルを使う。
- グループを明示的に割り当てると全子図形へ適用する。異なる画層の図形を単にグループ化した場合は、それぞれの所属を保持する。親の非表示・印刷不可は子にも適用する。ロックされた子を含むグループは操作対象から外す。
- 画層ロック/非表示は、一時的な表示・操作状態として解決する。オブジェクト自身のロック/表示と混同せず、画層を再表示・解除しても元の状態を保つ。
- 画層削除時は所属図形を画層0へ移し、個別属性へ戻す。図形自体は削除しない。画層変更・割当・削除はUndo/Redoの対象。
- 素材・内部クリップボードの別文書への挿入では、同じ定義を再利用し、同名で異なる定義には番号付きの別名を付ける。
- 非表示または印刷不可の図形はSVG/PNG/PDF/DXFと画像クリップボード出力から除外する。印刷不可だけなら編集画面には残る。

保存形式はschema v3。v1/v2を読み込むと画層0を補い、従来のスタイルを個別指定として維持する。JSON、自動保存、プロジェクト世代、素材、バックアップ、Undo/Redoに画層定義を含める。旧アプリにv3文書を読み込ませる用途には対応しない。

## DXF読み込み

CADモードの「DXF読込」→ファイル/文字コード選択→「内容を確認」→入力単位選択→「図面へ追加」。読込前に対応件数・除外理由・外接範囲を表示する。既存図形に追加し、追加画層も含めてUndo 1回で戻す。途中失敗は図形・画層を巻き戻す。

| 対象 | 初版の扱い |
|---|---|
| ファイル形式 | ASCII DXF R12、`$ACADVER=AC1009`。DWG・バイナリDXF・他バージョンは拒否 |
| LINE | XYの始終点。Z=0のみ |
| POLYLINE | 通常の2D VERTEX/SEQEND列、直線辺、開閉。bulge、幅、スプライン/曲線フィット、メッシュは除外 |
| CIRCLE | XY中心・正の半径 |
| TEXT | 左寄せ・通常ベースライン、挿入点・高さ・回転。中央/右寄せ、反転、斜体角、幅倍率の指定は除外 |
| 文字コード | UTF-8（ASCIIを含む）/Shift_JISを明示選択。日本語を両形式で検証。不正バイトは拒否 |
| 書体 | Arialと環境の日本語代替書体。SHX/STYLEの完全再現は行わず警告する。`%%`記法は文字列のまま |
| 画層 | 名前、基本ACI色1〜7、CONTINUOUS/DASHED/DOTTED、非表示・凍結・ロック |
| 色・線種 | ACIの基本7色以外は濃いグレー、未知の線種は実線に置換し警告する |
| 個別属性 | 明示色/線種が一つでもあればオブジェクト全体を個別指定として取り込む。BYBLOCKは除外 |
| 未対応 | BLOCK/INSERT、3D/厚さ/傾いた押出方向、ペーパー空間、ARC/ELLIPSE/SPLINE/LWPOLYLINE/MTEXT等。未対応要素は件数と理由を表示 |

入力単位はmm/cm/m/inch。`$INSUNITS`が対応単位を示す場合は初期選択するが、確認して変更できる。単位がなければ選択を必須にする。`$MEASUREMENT`のメートル系フラグからmmを推定しない。

内部座標は実寸mm。`x = DXF.x × 単位倍率`、`y = CAD用紙高さ − DXF.y × 単位倍率`で変換し、負座標と相対位置を保持する。インポート後は表示倍率を調整するが図形を原点へ移動しない。プレビューのTEXT範囲は挿入点のみで、文字外形ではない。

上限は20 MiB、5,000要素、100,000頂点、600,000コード/値ペア、200画層、各文字列4,096文字。入力数値とmmへの変換結果は絶対値10億以内。独立Workerで解析し、キャンセル/閉じる/15秒タイムアウトでWorkerを終了する。準備済みのFabric図形だけをまとめて追加する。

## DXF出力と往復の限界

LINE/POLYLINE/CIRCLE/TEXTの座標、寸法、回転について、mm/cm/m/inchで変換して再出力した値を小数点以下5桁の許容誤差でテストしている。閉POLYLINEは編集可能なPolygonとなり、再出力時も閉POLYLINEに戻る。

画層名・所属・基本色・線種をR12へ出力する。非ASCII画層名はASCIIの一意な名前に置換し、日本語TEXTも従来どおり`?`へ置換して警告する。任意RGB色は基本色へ近似する。R12の初版範囲では線幅・印刷可否属性を保存せず、印刷不可図形そのものを除外する。したがって、編集データの完全な受け渡しにはschema v3のJSONまたはバックアップを使う。

## 実装方式の検討と参照

[dxf-parser](https://github.com/gdsestimating/dxf-parser)はMITライセンスでブラウザ利用・画層を含む広い2D解析に対応する候補。[dxf-viewer](https://github.com/vagran/dxf-viewer)はMPL-2.0で、Three.jsによる閲覧とWorkerへの処理分離を提供する候補として確認した。後者は閲覧を中心とし、既存Fabricの編集モデルと描画系が重なる。今回は対応要素と入力上限を明確に制御するため、仕様資料に基づく限定パーサーを実装し、新規の外部依存は追加していない。候補のコードは転用していない。

一次資料:

- [Autodesk DXF group codes](https://help.autodesk.com/cloudhelp/2024/ENU/AutoCAD-DXF/files/GUID-3F0380A5-1C15-464D-BC66-2C5F094BCFB9.htm)
- [Autodesk POLYLINE](https://help.autodesk.com/cloudhelp/2016/ENU/AutoCAD-DXF/files/GUID-ABF6B778-BE20-4B49-9B58-A94E64CEFFF3.htm)
- [Autodesk VERTEX](https://help.autodesk.com/cloudhelp/2021/ENU/AutoCAD-DXF/files/GUID-0741E831-599E-4CBF-91E1-8ADBCFD6556D.htm)
- [Autodesk TEXT](https://help.autodesk.com/cloudhelp/2024/ENU/AutoCAD-DXF/files/GUID-62E5383D-8A14-47B4-BFC4-35824CAE8363.htm)
- [Autodesk LAYER](https://help.autodesk.com/cloudhelp/2018/ENU/AutoCAD-DXF/files/GUID-D94802B0-8BE8-4AC9-8054-17197688AFDB.htm)
